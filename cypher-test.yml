- hosts:
    - "{{ morpheus['instance']['hostname'] + '.' + morpheus['instance']['domainName'] }}"
  gather_facts: true
  tasks:
      - name: Process JSON
        debug:
          msg: "Good JSON is {{ lookup('cypher','secret=secret/jsontestgoodjson') | from_json }}"
