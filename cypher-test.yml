- name:  Retrieve JSON from Cypher
  hosts: uthman-6.0.6-test.localdomain
  gather_facts: false
  tasks:
     tasks:
      - name: Process JSON
        debug:
          msg: "Bad JSON: {{ lookup('cypher','secret=secret/jsontest') }}" + "Good JSON: {{ lookup('cypher','secret=secret/jsontest') }}"
